extends layout

block content
  h1= title
  p Welcome to #[em HelBikApp], a very basic Express website developed as a tutorial example on the Mozilla Developer Network.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The database has the following record counts:

    ul
      li #[strong Books:] !{data.journey_count}
      li #[strong Copies:] !{data.journey_instance_count}